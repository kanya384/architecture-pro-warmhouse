server:
  port: 8083
spring:
  application:
    name: devices
  datasource:
    url: jdbc:postgresql://telemetry-db:5432/telemetry
    username: postgres
    password: postgres
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.xml